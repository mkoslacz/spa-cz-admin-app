# Operator findings — SPA.CZ availability and package management

**Generated:** 2026-08-08 08:54 CEST
**Source:** Operator request from 2026-08-08
**Target:** `spa-cz-admin-app` mobile product screens, generated source, and interaction tests

## Findings

| ID | Severity | Area | Finding | Evidence | Required direction |
|---|---|---|---|---|---|
| OP-01 | Medium | Product copy | The mobile product repeatedly tells the partner that records, capacity, packages, prices, accounts, and actions are “demo” or “ukázka”. The review panel also exposes “Demo inventory”. This is redundant prototype framing in the product itself and obscures the working interface; the deterministic-fixture provenance belongs in research and source documentation instead. | `rg -o -i '\\b(demo|ukázka|ukázkov[[:alpha:]]*|DEMO-[0-9]+)\\b' m-*.html proto-m.js tools/build-screens.js \| wc -l` returns `491`; live Availability visibly contains “Ukázková data”, “Ukázková kapacita”, and room rows ending in “demo”. | Remove these markers from every rendered mobile screen, its sheets, toasts, account/review controls, route-visible record IDs, and published use-case content. Do not replace them with claims that fixture data is live, public, or verified; retain provenance only outside product UI. |
| OP-02 | Medium | Availability — single-cell editing | A tap on a room/date cell merely switches its text between the original number and `×`. It cannot set a numeric availability, has no room/date context, does not distinguish numeric zero from stop sell, and does not retain a changed value outside that DOM toggle. | `proto-m.js:583-603`; live test: a cell reading `4` became `×`; `tools/build-screens.js:405-443` emits plain cells without room/date identifiers. The SPA.CZ source screen accepts an availability number in the `0–255` range and treats stop sell as a separate operation: `spacz_web/www/extranetspacz/ts/pages/availability-overview/index.ts:121-239,290-303`; `app/models/AvailableCapacity/Set.php:53-135`. | Give each cell a real “Set availability” flow with the room type, date, current numeric value, validation, separate stop-sell state, and a visible saved result. `0` must remain numeric zero; `×` must mean stop sell. Changes must survive navigation/reload in prototype state and respect read-only and Channel Manager write restrictions. |
| OP-03 | Medium | Availability — bulk editing | The only bulk action is “Close a date range”. Its form has dates plus All rooms/Double, but no action selector or quantity; submission only closes the sheet and shows a toast without changing any matrix value. | `tools/build-screens.js:445-453,699-706`; `proto-m.js:624-631`; live sheet has two date inputs and zero numeric inputs. | Provide one bulk flow with an explicit choice between “Set available units” and “Stop sell”, inclusive date range, all or a named room type, numeric input when required, a pre-submit scope/count, and an applied result that changes exactly the selected cells. The action must retain the single-cell distinction between `0` and stop sell. |
| OP-04 | Medium | Domain model and explanatory UI | Double, Deluxe Double, and Suite are room types, while a stay package is a separate sellable product. The source SPA.CZ model links package prices to room types and seasons; the prototype instead has two disconnected hard-coded room lists and package fixtures without room-type links. The rate screen therefore makes the same labels look like a second form of package inventory. | SPA.CZ source: availability is `ObjectKind`/`obk_id` by date, while package prices are `PackagePrices(pac_id, obk_id, pas_id)`: `spacz_web/app/controllers/extranetspacz/AvailabilityOverviewController.php:40-80`; `app/models/Package/Price/Set.php:86-126`. Prototype: `tools/build-screens.js:405-443,477-484`; `OFFERS` at `:88-133`; `proto-m.js:203-230`. | Model room types once and reference them from package prices/eligibility. Availability must manage only room-type inventory; package editing must call the table “Package prices by room type”, show its covered room types, and make clear that package content does not mutate inventory while inventory constrains sale of linked packages. |
| OP-05 | Medium | Package creation | The only entry point for a new package is an unlabeled pencil icon. The form asks only for name and nights; submitting it produces a toast but no new package, no editable record, and no route to the created item. | `tools/build-screens.js:464-474,716-725`; live Offer has a button with no visible label and its sheet has two fields; `tools/test-mobile-browser.js:372-378` asserts only sheet closure and a toast. | Replace the icon-only entry point with a visible text action such as “Add package”. Creation must make a draft package appear on the list and open that package’s own editor in the same prototype session, with identity preserved on return/reload. |
| OP-06 | Medium | Package editing | Existing package cards expose only “Rates”. The available edit sheets cover name, stay length, and publication status, but do not update the selected package or offer the product fields the user needs: description, images/gallery, inclusions, meal, dates, room prices, procedures, and applicable settings. | `tools/build-screens.js:456-461,487-503,727-746`; `proto-m.js:203-230,624-631`. The SPA.CZ package form has corresponding areas for basic content, gallery/photos, dates/prices, procedures, settings, and conditional cancellation: `spacz_web/app/views/scriptsextranetspacz/packages/form/base.tpl:13-145`; `terms.tpl:12-80`; `procedures.tpl:8-17`; `settings.tpl:13-80`; `cancellation.tpl:9-18`. | Give each package a named “Edit package” destination distinct from “Rates”. The mobile editor must support package-specific content and settings backed by the shared fixture model: name, description, existing gallery image selection, inclusions, nights, meal, covered room types and prices, publication, procedures, and applicable settings. After save, the edited card/detail must show the selected package’s changed values rather than the first fixture’s values. |

## Expected effects

| Finding | Observable | Read how | Value at emission |
|---|---|---|---|
| `docs/audits/operator-findings-20260808-0854-spacz-availability-packages.md#OP-01` | Rendered product copy has no `demo`, `ukázka`, or `DEMO-` marker while research/provenance remains outside the product UI. | Run the generated-product copy assertion and browser text/accessibility sweep added to the existing quality suite, then run `rg -n -i '\\b(demo|ukázk|DEMO-)\\b' m-*.html index.html usecases.built.json`. | `491` broad generated/runtime markers; at least nine visible markers on the Offer screen and several on Availability. |
| `docs/audits/operator-findings-20260808-0854-spacz-availability-packages.md#OP-02` | A chosen room/date cell can be saved as a number, numeric `0`, or separate stop sell; the exact state persists and write restrictions remain enforced. | Extend `tools/test-mobile-browser.js` to set `4 → 3`, reload, set `0`, set stop sell, and repeat under read-only and Channel Manager states in both languages. | A live click changes `4 → ×`; no numeric editor; only the Channel Manager block is tested. |
| `docs/audits/operator-findings-20260808-0854-spacz-availability-packages.md#OP-03` | Bulk “Set available units” and “Stop sell” alter only the selected room types and inclusive dates, with an accurate affected-cell count and retained state. | Extend `tools/test-mobile-browser.js` with Double + 16–17 October + `3` and All rooms + two dates + stop sell; assert the exact changed and unchanged cells after reload. | One close-range form; `2` date inputs, `0` numeric inputs, and no matrix mutation after submit. |
| `docs/audits/operator-findings-20260808-0854-spacz-availability-packages.md#OP-04` | Every package price row names a known room type, availability has one room-type source, and the selected package screen explains the relationship and sale constraint. | Add a product-model contract test that validates unique room type IDs, package room-price references, room-type coverage, and no duplicate hard-coded rate/availability row definitions; exercise the labelled rate screen in `tools/test-mobile-browser.js`. | Availability and rate tables are separate literal arrays; `OFFERS` has no room-type relation. |
| `docs/audits/operator-findings-20260808-0854-spacz-availability-packages.md#OP-05` | A visible “Add package” action creates a draft, shows it in the list, and opens the created package’s own editor with preserved identity. | Extend `tools/test-mobile-browser.js` to create a named package, assert its card and route identity, reload/navigate back, and assert the same draft is selected. | The icon-only form has two fields and ends in a toast; the list remains four static cards. |
| `docs/audits/operator-findings-20260808-0854-spacz-availability-packages.md#OP-06` | Every package has a clear edit action and a package-specific editor for content, gallery selection, offer details, room prices, publication, procedures, and applicable settings. | Extend `tools/test-mobile-browser.js` to edit title, description, gallery selection, inclusion, status, and a room price for a non-first package; assert the same record renders changed values after return/reload. | Only name/stay length and publication status forms exist; hydration always derives price rows from the first fixture plus fixed deltas. |

## Handoff

- Source: `operator request from 2026-08-08`
- Scope: `OP-01`, `OP-02`, `OP-03`, `OP-04`, `OP-05`, `OP-06`
- State: `reviewed`
- Evidence: `9306e4f9c81bb7ca2760422d63de146b6bc32a71`; live mobile inspection at `https://mateuszkoslacz.com/spa-cz-admin-app/m-availability.html`; source and browser-test readings cited above
- Next: `/impl-guide docs/audits/operator-findings-20260808-0854-spacz-availability-packages.md medium`
