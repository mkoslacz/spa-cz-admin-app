# Operator findings — SPA.CZ mobile prototype interaction round

**Source:** Operator feedback from 2026-08-07, inspected against `a662f650b2e3a91466308c95e5a0d3c5ea5fbde9`  
**Generated:** 2026-08-07 21:03 CEST  
**Scope:** Mobile product interactions, review use-case authoring, and Firebase comment setup

## Findings

| ID | Severity | Area | Finding | Evidence | Required direction |
|---|---|---|---|---|---|
| OP-01 | High | Use-case workshop | The review page renders only the eight hand-authored canonical scenarios. It has no create, edit, duplicate, delete, import, export, validation, or local draft workflow, while the static test hard-codes `length === 8`. The generator itself can accept an arbitrary list, so the limit is introduced by the review surface and QA rather than by the prototype contract. | `usecases.html:115-389`; `tools/build-usecases.js:95-135,416-438`; `tools/test-mobile.js:95-105`; live `usecases.html` shows eight read-only cards. | Turn the Use cases area into a safe static workshop for many scenarios: schema-backed editing, locally persisted drafts, import/export, deep-link preview, scalable capture behavior, and no collision with stable Firebase comment anchors on canonical cards. |
| OP-02 | High | Dashboard and record routing | Four dashboard KPI cards are static articles; the three attention rows expose only an effectively zero-sized chevron link instead of a full touch target. Record identity is also not honored: `reservation=DEMO-10477` still renders the hard-coded DEMO-10482 detail. | `tools/build-screens.js:130-169,242-290`; `app-m.css:155-165,507-528`; `proto-m.js:14-24,103-151`; live `m-dashboard.html` accessibility tree. | Make complete cards and rows semantic destinations with preserved state and meaningful filters, and make reservation/offer identity determine the destination content. |
| OP-03 | Medium | Product copy and provenance | Labels such as “Public offer fact”, “Public fact”, “Public baseline” and “publicly verified facts” are hand-authored prototype framing, not SPA.CZ partner-product concepts. They appear 27 times across source, generated pages and use-case documentation, despite the underlying data being only a deterministic demo fixture. | `tools/build-screens.js:156-160,255-280,344-388`; `usecases.json:220`; 27 matches from the repository phrase scan at emission. | Remove the invented fact/baseline framing from product UI and scenarios. Keep the package and price only as clearly marked demo data, while provenance remains in research documentation rather than becoming a product label. |
| OP-04 | High | More and settings | Thirteen of fourteen tiles in More use `href="#"` plus a generic toast. Users and Permissions additionally emit duplicate `class` attributes, and the declared role filtering has no runtime or style implementation. The screen therefore advertises rooms, gallery, profile, documents, users, permissions, channel manager, settings, approvals and help without any meaningful result. | `tools/build-screens.js:415-443`; generated `m-more.html:41-60`; `proto-m.js:221-228`; live `m-more.html` accessibility tree. | Replace the placeholder factory with a required outcome contract: real route, contextual bottom sheet/form, or explicit terminal result. Cover every tile, role state, notification entry point, focus/close behavior and state preservation. |
| OP-05 | High | Interaction quality gate | Existing static and browser QA can stay green while visible controls are dead, tiny, route to the wrong record, or only toggle appearance. It does not click dashboard KPIs/tasks, More tiles, offer filters/cards, property selection, signed-out CTA, notification control, or validate touch target geometry and exact outcomes. | Baseline `npm --prefix tools test`, lint and escalated browser QA pass at `a662f65`; `tools/test-mobile-browser.js:53-177` covers chrome/state/sheet presence but none of the failing outcomes. | Add a no-dead-affordance contract and HTTP browser tests that exercise Czech and English outcomes, minimum touch targets, exact path/query/state, sheets and focus, filtering, identity, error-free console, and a static ban on placeholder anchors/toasts. |
| OP-06 | Medium | Firebase comments setup | The prior Firebase device session expired and the authorization server reported a malformed request. In this environment the CLI cannot write its normal global config inside the sandbox and agent detection selects a split non-interactive login path; starting and completing that path in different process/config contexts loses the pending verifier. The README does not document a one-process recovery path. | `firebase-tools` 15.26.0; no authorized accounts; CLI reports the global config write failure; no `.firebaserc` or `firebase-debug.log`; `README.md:141-150` only describes the post-login configuration. | Document and use one escalated interactive PTY with a private temporary `XDG_CONFIG_HOME`, verify the account in the same process context, then configure a dedicated project, rules, allowlist, web app, validated local config and GitHub secret. |

## Expected effects

| Finding | Observable | Read how | Value at emission |
|---|---|---|---|
| `docs/audits/operator-findings-20260807-2103.md#OP-01` | Use-case authoring capacity and round-trip behavior | Run the use-case contract and HTTP browser tests; create/import at least 100 valid scenarios, reload, export and re-import, then compare count and normalized JSON. | Eight canonical read-only cases; no authoring controls; CI rejects a ninth case. |
| `docs/audits/operator-findings-20260807-2103.md#OP-02` | Dashboard action matrix and routed record identity | Run the product interaction browser test and inspect its KPI/task destination and `DEMO-10477` detail assertions. | Four non-interactive KPI articles; task chevrons have no reliable touch box; detail content is hard-coded to DEMO-10482. |
| `docs/audits/operator-findings-20260807-2103.md#OP-03` | Unsupported public-fact framing in product and generated use-case artifacts | Run `rg -n -i "public offer fact|public fact|public baseline|publicly verified|public fixture|verified pricing baseline|veřejný fakt|veřejný základ|veřejně ověřen|ověřený cenový základ" m-*.html tools/build-screens.js usecases.json usecases.built.json docs/usecases.md README.md` and count matches. | 26 product/generated/documentation matches; research provenance is intentionally excluded from the target count. |
| `docs/audits/operator-findings-20260807-2103.md#OP-04` | More tile outcome coverage | Run static and browser interaction tests; compare the number of visible More tiles with the number that produce exactly one verified route, sheet/form, or terminal result in both languages and permission modes. | 14 visible tiles, one real route and 13 `href="#"` placeholder toasts per language; role filtering does not work. |
| `docs/audits/operator-findings-20260807-2103.md#OP-05` | Automated visible-affordance outcome coverage | Run the full static/lint/format/browser suite and inspect named assertions for every generated dashboard/More control plus shared header/auth/property controls. | Baseline suite passes while the defects in OP-02 and OP-04 remain observable in the live application. |
| `docs/audits/operator-findings-20260807-2103.md#OP-06` | Reproducible Firebase login and configuration handoff | Follow the repository runbook in one terminal, then read `firebase login:list`, `firebase projects:list`, rules deployment result, config validator result, secret presence and live Comments authentication state. | No authorized Firebase account; previous one-time session is expired/malformed; comments remain disabled. |

## Handoff
- Source: `operator request`
- Scope: `OP-01`, `OP-02`, `OP-03`, `OP-04`, `OP-05`, `OP-06`
- State: `reviewed`
- Evidence: `a662f650b2e3a91466308c95e5a0d3c5ea5fbde9`; baseline static/lint/format passed; browser QA passed only after Chrome sandbox escalation; live accessibility inspection reproduced the missing outcomes
- Next: `/impl-guide docs/audits/operator-findings-20260807-2103.md`
